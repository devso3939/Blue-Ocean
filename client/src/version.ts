// Single source of truth for the app version — imported by every view so the
// version stamp is ALWAYS visible no matter which screen you're on. Bump this
// on every deploy together with client/index.html and client/package.json.
export const APP_VERSION = '6.9.74';
